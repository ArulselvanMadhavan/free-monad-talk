package com.composter.talks.freemonad

object Main {
  def main(args: Array[String]): Unit = {
    // Example.addNumbersToList(Range(0, 10000, 1).toList)
    // println(Example.addNumbersWithTrampolinedState(Range(0, 100000, 1).toList).length)

    // println(Example.unsafeEven(100000000))
    // println(Example.safeEven(100000000).runT)
    // println(Example.unsafeFac(100000))
    // println(Example.safeFac(10000000).runT)
    // Example.addNumbersToList(Range(0, 100000, 1).toList)
    // println(Example.addNumbersWithTrampolinedState(Range(0, 1000000, 1).toList).length)
    // println(Example.addNumbersWithStateF(Range(0, 10000000, 1).toList).length)
  }
}
